What is an API?

 APIs are mechanisms that enable two software components — such as the frontend and backend of an application — to communicate with each other.
 They use a defined set of rules, protocols, and data formats.
 The frontend sends a request through the API.
 The backend processes it and sends a **response** back through the API.
